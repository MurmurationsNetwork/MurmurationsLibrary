{
  "title": "Street Address",
  "description": "The street address of the entity in a single text field as you would write it on an envelope",
  "type": "string",
  "metadata": {
    "creator": {
      "name": "Murmurations Network",
      "url": "https://murmurations.network"
    },
    "field": {
      "name": "street_address",
      "version": "0.0.1"
    },
    "context": [
      "https://schema.org/street_address"
    ],
    "purpose": "Street address captures the physical address of an entity, without the town/city, postcode, etc. This is useful for mapping and other applications where the full address is not required."
  }
}
