{
  "title": "Images",
  "description": "A URL for an image of the item, a logo, etc.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Image Name",
        "description": "Describe the image",
        "type": "string",
        "minLength": 1,
        "maxLength": 100
      },
      "url": {
        "title": "URL",
        "description": "A URL of the image starting with http:// or https://",
        "type": "string",
        "maxLength": 2000,
        "pattern": "^https?://.*"
      }
    },
    "required": [
      "url"
    ]
  },
  "metadata": {
    "creator": {
      "name": "Murmurations Network",
      "url": "https://murmurations.network"
    },
    "field": {
      "name": "images",
      "version": "0.0.1"
    },
    "context": [
      "https://schema.org/image"
    ]
  }
}
